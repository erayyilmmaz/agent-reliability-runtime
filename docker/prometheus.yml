global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/security-alerts.yml

scrape_configs:
  - job_name: agent-reliability-runtime
    static_configs:
      - targets: ["otel-collector:8889"]
